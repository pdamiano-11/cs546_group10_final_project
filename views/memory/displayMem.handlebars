<h3 id="display_title">{{title}}</h3>
<h3 id="display_description">{{description}}</h3>
<h3 id="display_date">{{date}}</h3>
<h3 id="display_location">{{location}}</h3>
<div id="map"></div>
<h3 id="display_visibility">{{visibility}}</h3>

<script>
    function initMap() {
        var input = document.getElementById('location');
        alert(input);
        var autocomplete = new google.maps.places.Autocomplete(input);
        var city, lat, lng;
        new google.maps.event.addListener(autocomplete, 'place_changed', function () {
            var place = autocomplete.getPlace();
            city = place.name;
            lat = place.geometry.location.lat();
            lng = place.geometry.location.lng();
            alert(city);

        });
        const loc = { lat: lat, lng: lng };
        alert(loc.lat);
        // The map, centered at Uluru
        const map = new google.maps.Map(document.getElementById("map"), {
          zoom: 4,
          center: loc,
        });
        // The marker, positioned at Uluru
        const marker = new google.maps.Marker({
          position: loc,
          map: map,
        });
    }
    new google.maps.event.addDomListener(window, 'load', initMap);
</script>

<a href = "/memory" id="display_create_memory">Create a new Memory</a>
<a href = '/memory/update/{{id}}' id="display_update_memory">Update this Memory</a>
<a href = "/memory/delete/{{id}}" id="display_delete_memory">Delete this Memory</a>
<a href = "/profile">Return to Profile</a>