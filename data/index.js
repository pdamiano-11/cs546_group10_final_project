const userData = require('./restaurants');
const memoryData = require('./reviews');
const dateData = require('./dates');

module.exports = {
  users: userData,
  memories: memoryData,
  dates: dateData
};